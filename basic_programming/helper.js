const sum = (a, b) => a + b;
const difference = (a, b) => a - b;
const product = (a, b) => a * b;
const quotient = (a, b) => a / b;

const computeAverage = (a, b) => a + b / 2;

export { sum, difference, product, quotient, computeAverage };  