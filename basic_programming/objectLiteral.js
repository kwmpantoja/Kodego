let vehicle_name = 'Max';
let color = 'Green';
let year = '2022';
let name = '';
const car = {
  vehicle_name,
  color,
  year,
};

const studentsInfo = {
  name: 'Student',
  vehicle_name: 'Toyota',
};

let abc = 'Hello';
let def = 'Kodego';

let welcomeMessage =
  `Welcome and ${studentsInfo.name}    fsdnsoidfn isdjfds  ${studentsInfo.vehicle_name}`;
console.log(welcomeMessage);